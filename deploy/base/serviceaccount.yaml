apiVersion: v1
kind: ServiceAccount
metadata:
  name: simple-host
# The process never talks to the Kubernetes API. Platforms that inject cloud
# credentials through a workload identity annotate this account in their
# overlay; the token they inject is theirs, not the API server's.
automountServiceAccountToken: false
